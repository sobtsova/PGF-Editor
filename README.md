# 📈 PGF GenService

**Веб-сервіс для автоматизованого формування графіків у форматі PGF для системи LaTeX.**

> **Курсова робота**<br>
> **Студентка:** Собцова Софія Олександрівна<br>
> **Група:** ІП-34<br>
> **Навчальний заклад:** Національний технічний університет України "Київський політехнічний інститут імені Ігоря Сікорського"<br>
> **Рік:** 2025

---

## 📝 Про проєкт

**PGF GenService** — це спеціалізований веб-інструмент, розроблений для науковців, інженерів та студентів. Він вирішує проблему складного ручного кодування графіків у середовищі LaTeX (пакет `pgfplots`).

Сервіс дозволяє генерувати валідний код графіків через зручний графічний інтерфейс (GUI), використовуючи різні джерела даних, та миттєво переглядати результат.

### ✨ Ключові можливості

* **Три режими введення даних:**
    1.  ✍️ **Ручне введення:** Поточкове додавання координат через таблицю.
    2.  🧮 **За функцією:** Автоматична дискретизація математичних формул (наприклад, `y = sin(x) * x`) з налаштуванням діапазону та кроку.
    3.  📂 **Імпорт:** Завантаження великих масивів даних із файлів форматів `.csv` та `.json`.
* **Live Preview:** Попередній перегляд графіка в реальному часі (WYSIWYG) ще до генерації коду.
* **Кастомізація:** Налаштування заголовків, підписів осей, кольорів (HEX), типів ліній (суцільна/пунктирна/точкова) та сітки.
* **Експорт:** Можливість скопіювати код у буфер обміну або завантажити готовий файл `.pgf`.
* **Зручність:** Темна тема (Dark Mode), адаптивний інтерфейс, збереження налаштувань сесії (LocalStorage).

---

## 🛠 Технологічний стек

Проєкт побудовано на базі дволанкової клієнт-серверної архітектури (Stateless).

### Backend (Серверна частина)
* **Node.js** — середовище виконання.
* **Express.js** — веб-фреймворк для реалізації REST API.
* **Алгоритм:** Шаблонна генерація тексту (Template Injection).

### Frontend (Клієнтська частина)
* **HTML5 / CSS3** — структура та стилізація (використання CSS Variables).
* **JavaScript (ES6+)** — логіка взаємодії.
* **Chart.js** — бібліотека для візуалізації попереднього перегляду.
* **Math.js** — бібліотека для безпечного парсингу та обчислення математичних виразів.

### Інструменти розробки та тестування
* **VS Code** — IDE.
* **Postman** — тестування API.
* **Apache JMeter** — навантажувальне тестування.
* **CodeFactor** — статичний аналіз коду.

---

## 🚀 Інструкція зі встановлення та запуску

Для запуску проєкту локально необхідно мати встановлений **Node.js** (версії 14.0 або вище).

### Крок 1. Завантаження проєкту
Скопіюйте файли проєкту у локальну папку.

### Крок 2. Встановлення залежностей
Відкрийте термінал у папці проєкту та виконайте команду:
```bash
npm install
```

### Крок 3. Запуск сервера
Запустіть веб-сервер командою:
```bash
node server.js
```

### Крок 4. Робота
Відкрийте браузер і перейдіть за адресою: http://localhost:3000

## 📂 Структура проєкту

```text
pgf-genservice/
├── node_modules/       # Бібліотеки та залежності
├── public/             # Статичні файли клієнта
│   ├── index.html      # Головна сторінка (Меню)
│   ├── manual.html     # Сторінка ручного введення
│   ├── function.html   # Сторінка розрахунку за функцією
│   ├── import.html     # Сторінка імпорту файлів
│   ├── result.html     # Сторінка відображення коду та експорту
│   ├── style.css       # Таблиця стилів (Dark/Light themes)
│   └── script.js       # Логіка клієнта
├── server.js           # Точка входу сервера (API /api/generate)
├── package.json        # Конфігурація проєкту
└── README.md           # Документація
```

## 📊 Приклад використання (LaTeX)
Згенерований код призначений для вставки у середовище LaTeX.

Вимоги: У преамбулі вашого .tex файлу має бути підключений пакет:
```bash   
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
```

Приклад коду, що генерує сервіс:
```bash
\definecolor{myGraphColor}{HTML}{10B981}
\begin{tikzpicture}
    \begin{axis}[
        title={Графік Синуса},
        xlabel={X},
        ylabel={Y},
        grid=major,
        width=12cm,
    ]
    \addplot[color=myGraphColor, dashed] coordinates {
        (-5,0.96) (-4.5,0.98) ... (5,-0.96)
    };
    \end{axis}
\end{tikzpicture}
```
